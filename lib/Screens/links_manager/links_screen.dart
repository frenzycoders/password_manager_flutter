import 'package:flutter/material.dart';

class LinksManager extends StatelessWidget {
  const LinksManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CustomScrollView(
        slivers: [
          
        ],
      ),
    );
  }
}
